export default class {
    /**
     * 
     * @param {HTMLTableElement} root The Table Element that will display the CSV data
     
     */
    constructor(root) {
        this.root = root;
        console.log("I am constructed");
    }
}